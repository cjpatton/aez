AEZ builds for three different architectures: 32-bit x86 (default), 64-bit x86,
and 64-bit x86 with support for AES-NI. For example, to build benchmarks for 
AES-NI, do 

  $ make bm arch=aes-ni 

Or, similarly, arch=x64. Omitting the `arch` argument defaults to 32-bit. To
install the python wrapper, do

  $ sudo make arch=aes-ni install 
  $ sudo ldconfig

Then you should be able to `import aez` in Python.

